import styled from "styled-components";
import Button from "../Button";

export const MobileLoginButton = styled(Button)`
	position: relative;
	width: 9rem;
	margin: 1.2rem 2rem 2.4rem 0;
	padding: 0.8rem;
`;
